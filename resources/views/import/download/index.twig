{% extends "./layout/default" %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                &nbsp;
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <h1>{{ mainTitle }}</h1>
                <h2>{{ subTitle }}</h2>
            </div>
        </div>
        <div id="app">
            <download-status></download-status>
        </div>
    </div>
{% endblock %}
{% block scripts %}
    <script type="text/javascript">
        var downloadStatusUri = '{{ route('import.download.status') }}?identifier={{ identifier }}';
        var downloadStartUri = '{{ route('import.download.start') }}?identifier={{ identifier }}';
        var configDownloadUri = '{{ route('import.configuration.download') }}?identifier={{ identifier }}';
        var flushUri = '{{ route('flush') }}';
    </script>
    <script src="js/download.js" type="text/javascript"></script>
{% endblock %}
